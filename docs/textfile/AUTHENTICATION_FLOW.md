# 🔄 Authentication Flow Diagram

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                    GYM MANAGEMENT SYSTEM                            │
│                  Authentication Architecture                         │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│              │
│  User visits │
│  any page    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  auth-check.js (runs automatically on every page)                │
│                                                                   │
│  1. Check for token in localStorage/sessionStorage               │
│  2. If no token → Redirect to login.html                         │
│  3. If token exists → Verify with server                         │
│  4. If valid → Load page + show user info                        │
│  5. If invalid → Clear token + Redirect to login                 │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌────────────────────────────────────────────────────────────────┐
│                                                                 │
│  Two Authentication Paths:                                      │
│                                                                 │
│  ┌──────────────────────┐      ┌─────────────────────────┐    │
│  │  Email/Password      │      │  Google OAuth           │    │
│  │  Login               │      │  Sign-In                │    │
│  └──────────────────────┘      └─────────────────────────┘    │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

## 📧 Email/Password Login Flow

```
┌─────────────┐
│   User      │
│             │
│ Clicks      │
│ "Sign In"   │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│  login.html                     │
│                                 │
│  1. User enters email/password  │
│  2. Form submitted              │
│  3. login.js processes form     │
└──────┬──────────────────────────┘
       │
       │ POST /api/auth/login
       ▼
┌───────────────────────────────────────────────────────┐
│  server.js - Login Route                              │
│                                                        │
│  1. Find user by email                                │
│  2. Check if user exists                              │
│  3. Verify password (bcrypt.compare)                  │
│  4. Check if account is active                        │
│  5. Update last login timestamp                       │
│  6. Generate JWT token (sign with secret)             │
│  7. Return token + user info                          │
└──────┬────────────────────────────────────────────────┘
       │
       │ Response: { token, user }
       ▼
┌─────────────────────────────────┐
│  login.js                       │
│                                 │
│  1. Receive token from server   │
│  2. Store in localStorage       │
│     (or sessionStorage)         │
│  3. Store user info             │
│  4. Redirect to dashboard       │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  dashboard.html                 │
│                                 │
│  1. auth-check.js verifies token│
│  2. Show user name in header    │
│  3. Load dashboard content      │
│  4. User is logged in! ✅       │
└─────────────────────────────────┘
```

## 🔵 Google OAuth Login Flow

```
┌─────────────┐
│   User      │
│             │
│ Clicks      │
│ "Google"    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│  login.html                         │
│                                     │
│  1. Click "Sign in with Google"     │
│  2. Redirect to Google              │
└──────┬──────────────────────────────┘
       │
       │ GET /api/auth/google
       ▼
┌───────────────────────────────────────────────────┐
│  Google OAuth Server                              │
│                                                    │
│  1. User sees Google login screen                 │
│  2. User logs in with Google account              │
│  3. User grants permissions                       │
│  4. Google redirects back with code               │
└──────┬────────────────────────────────────────────┘
       │
       │ GET /api/auth/google/callback?code=...
       ▼
┌───────────────────────────────────────────────────┐
│  server.js - Google Callback                      │
│                                                    │
│  1. Passport exchanges code for user info         │
│  2. Check if user exists (by googleId)            │
│  3. If exists → Update last login                 │
│  4. If not → Check email, link or create user     │
│  5. Generate JWT token                            │
│  6. Redirect to frontend with token               │
└──────┬────────────────────────────────────────────┘
       │
       │ Redirect: /login.html?token=...&name=...
       ▼
┌─────────────────────────────────┐
│  login.js                       │
│                                 │
│  1. Extract token from URL      │
│  2. Store in localStorage       │
│  3. Store user info             │
│  4. Redirect to dashboard       │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  dashboard.html                 │
│                                 │
│  User is logged in with Google! │
│  ✅                             │
└─────────────────────────────────┘
```

## 📝 Registration Flow

```
┌─────────────┐
│   User      │
│             │
│ Clicks      │
│ "Sign Up"   │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│  register.html                      │
│                                     │
│  1. User fills registration form    │
│     - Name                          │
│     - Email                         │
│     - Password                      │
│     - Confirm Password              │
│     - Role (Member/Coach/Admin)     │
│  2. Form validated                  │
│  3. Submit registration             │
└──────┬──────────────────────────────┘
       │
       │ POST /api/auth/register
       ▼
┌───────────────────────────────────────────────────┐
│  server.js - Register Route                       │
│                                                    │
│  1. Validate input (name, email, password)        │
│  2. Check if email already exists                 │
│  3. Create new user document                      │
│  4. Password automatically hashed (pre-save hook) │
│  5. Save to MongoDB                               │
│  6. Return success + user info                    │
└──────┬────────────────────────────────────────────┘
       │
       │ Response: { message, user }
       ▼
┌─────────────────────────────────┐
│  register.js                    │
│                                 │
│  1. Show success message        │
│  2. Wait 2 seconds              │
│  3. Redirect to login.html      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  login.html                     │
│                                 │
│  User can now login! ✅         │
└─────────────────────────────────┘
```

## 🔒 Protected Page Access Flow

```
┌─────────────┐
│   User      │
│             │
│ Navigates   │
│ to page     │
└──────┬──────┘
       │
       ▼
┌────────────────────────────────────────────────────┐
│  Any Protected Page (dashboard, members, etc.)     │
│                                                     │
│  auth-check.js runs BEFORE page content loads      │
└──────┬─────────────────────────────────────────────┘
       │
       ▼
┌────────────────────────────────────────────────────┐
│  Token Check                                       │
│                                                     │
│  localStorage.getItem('token') ||                  │
│  sessionStorage.getItem('token')                   │
└──────┬─────────────────────────────────────────────┘
       │
       ├──── No Token? ───────────────────────────┐
       │                                          │
       │                                          ▼
       │                              ┌──────────────────────┐
       │                              │  Redirect to         │
       │                              │  login.html          │
       │                              │  ❌ Access Denied    │
       │                              └──────────────────────┘
       │
       ├──── Has Token ────────────────────────────┐
       │                                           │
       ▼                                           ▼
┌──────────────────────────────────┐   ┌─────────────────────────┐
│  Verify Token                    │   │  GET /api/auth/verify   │
│                                  │───│  Authorization: Bearer  │
│  Send token to server            │   │  <token>                │
└──────────────────────────────────┘   └─────────┬───────────────┘
                                                  │
                                                  ▼
                              ┌───────────────────────────────────┐
                              │  server.js - Verify Token         │
                              │                                   │
                              │  1. Extract token from header     │
                              │  2. Verify JWT signature          │
                              │  3. Check expiration              │
                              │  4. Find user in database         │
                              │  5. Check if user is active       │
                              │  6. Return user info              │
                              └──────┬────────────────────────────┘
                                     │
                        ┌────────────┴────────────┐
                        │                         │
                   Valid Token               Invalid Token
                        │                         │
                        ▼                         ▼
            ┌──────────────────────┐  ┌─────────────────────┐
            │  Load Page           │  │  Clear tokens        │
            │  - Show user name    │  │  Redirect to login   │
            │  - Show logout btn   │  │  ❌ Session expired  │
            │  - Grant access ✅   │  └─────────────────────┘
            └──────────────────────┘
```

## 🚪 Logout Flow

```
┌─────────────┐
│   User      │
│             │
│ Clicks      │
│ "Logout"    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│  auth-check.js - logout()       │
│                                 │
│  1. Get token from storage      │
│  2. Send logout request         │
└──────┬──────────────────────────┘
       │
       │ POST /api/auth/logout
       ▼
┌─────────────────────────────────┐
│  server.js - Logout Route       │
│                                 │
│  1. Verify token                │
│  2. Invalidate session          │
│  3. Return success              │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  auth-check.js                  │
│                                 │
│  1. Remove token                │
│  2. Remove user info            │
│  3. Redirect to login           │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  login.html                     │
│                                 │
│  Logged out successfully! ✅    │
└─────────────────────────────────┘
```

## 🔐 Token Structure

```
JWT Token Format:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjY5MjNmZmU2MW...
│                                 │                            │
└──────── Header ─────────────────┴──────── Payload ──────────┴─ Signature

Header (Base64):
{
  "alg": "HS256",
  "typ": "JWT"
}

Payload (Base64):
{
  "id": "6923ffe61f816193284dfb99",
  "email": "testuser@gym.com",
  "role": "member",
  "iat": 1763966960,    // Issued at timestamp
  "exp": 1764571760     // Expiration timestamp (7 days later)
}

Signature:
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  JWT_SECRET
)
```

## 🗄️ Database Structure

```
MongoDB Database: gym_management

Collections:
├── users (NEW!)
│   ├── _id: ObjectId
│   ├── name: String
│   ├── email: String (unique, indexed)
│   ├── password: String (hashed with bcrypt)
│   ├── role: String (member/coach/admin)
│   ├── googleId: String (optional, for OAuth)
│   ├── profilePicture: String (optional)
│   ├── authProvider: String (local/google)
│   ├── isActive: Boolean
│   ├── lastLogin: Date
│   ├── createdAt: Date
│   └── updatedAt: Date
│
├── members (existing)
├── coaches (existing)
├── payments (existing)
└── schedules (existing)
```

## 📱 State Management

```
Browser Storage:

localStorage (persistent):
├── token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
└── user: '{"id":"...","name":"John Doe","email":"...","role":"member"}'

sessionStorage (tab-only):
└── token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." (if not "remember me")

Token used for:
✅ Verifying user identity
✅ Accessing protected API endpoints
✅ Maintaining login state
✅ Authorizing actions by role
```

## 🛡️ Security Layers

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: Password Hashing                              │
│  - Bcrypt with 10 salt rounds                           │
│  - Passwords never stored in plain text                 │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 2: JWT Token Encryption                          │
│  - Signed with secret key                               │
│  - Cannot be tampered with                              │
│  - Expiration time enforced                             │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 3: Token Verification Middleware                 │
│  - Every protected route checks token                   │
│  - Invalid tokens rejected                              │
│  - Expired tokens rejected                              │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 4: Role-Based Authorization                      │
│  - Admin routes only for admin role                     │
│  - Coach routes for coach/admin                         │
│  - Member routes for authenticated users                │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 5: Frontend Protection                           │
│  - auth-check.js on every page                          │
│  - Automatic redirect if not authenticated              │
│  - Token verification on page load                      │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Complete Request Flow Example

```
User wants to view members list:

1. User clicks "Members" in sidebar
   ↓
2. Browser loads members.html
   ↓
3. auth-check.js runs automatically
   ↓
4. Checks for token in storage
   ↓
5. Sends verification request to server
   ↓
6. Server validates token (JWT verify)
   ↓
7. Server checks user in database
   ↓
8. Server returns user info
   ↓
9. Frontend updates UI with user name
   ↓
10. Page content loads
   ↓
11. JavaScript fetches members list:
    authenticatedFetch('/api/members')
    ↓
12. Request includes Authorization header:
    "Authorization: Bearer <token>"
    ↓
13. Server middleware verifies token again
    ↓
14. Server checks user role (authorization)
    ↓
15. Server returns members data
    ↓
16. Frontend displays members in table
    ↓
17. ✅ Success!
```

## 📊 Visual Page Structure

```
Every Protected Page:

┌────────────────────────────────────────────────────────┐
│  <script src="auth-check.js"></script>  ← Runs first  │
│  <script src="script.js"></script>                     │
└────────────────────────────────────────────────────────┘

Authentication Check:
1. Token exists? If NO → Redirect to login
2. Token valid? If NO → Clear & redirect
3. User active? If NO → Redirect to login
4. All checks pass? → Load page & show user info

User Menu (top-right):
┌──────────────────────────┐
│  John Doe    [Logout]    │
└──────────────────────────┘
   ↑              ↑
   │              └─ onClick="logout()"
   └─ Set by auth-check.js
```

---

## 💡 Key Takeaways

1. **Every page is protected** - auth-check.js runs automatically
2. **Token is the key** - Stored in browser, sent with every request
3. **Two login methods** - Email/password OR Google OAuth
4. **JWT security** - Signed, encrypted, time-limited
5. **Role-based access** - Different permissions for different roles
6. **Seamless flow** - User stays logged in across pages
7. **Automatic logout** - Invalid/expired tokens = auto redirect

---

This authentication system is **production-ready** and follows industry best practices! 🎉
